{
  "scripts": {
    "lint": "eslint -c .eslintrc src/**/*",
    "start": "node dist/app.js",
    "build": "tsc && npx ts-node ./createDistPackageJSON.ts",
    "deploy": "npm run build && gcloud run deploy choreme --project=choreme --region=us-central1 --port=8080 --source=./dist --allow-unauthenticated"
  },
  "dependencies": {
    "express": "^4.18.2",
    "typescript": "^5.2.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.20",
    "ts-node": "^10.9.1"
  }
}